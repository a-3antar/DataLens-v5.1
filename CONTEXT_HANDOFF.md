# Context Handoff — مشروع DataLens V5.0

## 🎯 الهدف العام
DataLens V5.0: تطبيق تحليل بيانات بلغة عربية/إنجليزية طبيعية، مبني بالكامل بـ **Python + Streamlit** (لا React). يرفع المستخدم ملفات Excel/CSV، يطرح أسئلة بلغة طبيعية، يحوّلها AI (Gemini/OpenRouter/Grok/Ollama محلي) إلى SQL يُنفَّذ عبر DuckDB، ويُعرض الناتج كجدول/رسم بياني/Gauge/KPI/تحليل نصي (Story Telling). النتائج تُجمَّع في تقارير قابلة للتصدير (PDF/Excel/Markdown)، ومؤخراً أُضيفت **لوحات معلومات تفاعلية (Dashboards)** بقوالب جاهزة وفلاتر (Slicers).

قاعدة بيانات لكل مشروع (`project.db` — SQLite)، وقاعدة مستخدمين مشتركة (`users.db`). الاستضافة مستهدفة: LAN + Docker (تطبيق + Ollama في حاوية واحدة معزولة).

**لا أملك ذاكرة بين المحادثات ولا وصولاً لملفات مشروعك الفعلية.** كل الملفات المذكورة أدناه بحاجة لإعادة رفعها في المحادثة الجديدة إن أردت الاستمرار بالتعديل عليها مباشرة.

---

## 📁 الحالة النهائية للملفات (Current State)
البنية الكاملة كما انتهينا إليها (تجاهل أي إصدار أقدم ذُكر أثناء النقاش):

```
datalens/
├── main.py                      # نقطة الدخول + تنقل + تنظيف تلقائي عند البدء (temp dirs + expired sessions)
├── config.py                    # يشمل: RESULT_TYPES (+story)، DASHBOARD_SLICER_COUNT=4، DASHBOARD_GAUGE_COUNT=4
├── requirements.txt             # matplotlib أُزيل، Pillow>=10 أُضيف صراحة
├── Dockerfile                   # صورة واحدة: app + Ollama معاً، tini كـ PID1، مستخدم غير جذري
├── docker-compose.yml           # منفذ 8501 فقط مكشوف، volume واحد معزول (users.db + المشاريع + نماذج Ollama)
├── docker/entrypoint.sh         # يشغّل ollama serve بالخلفية، ينتظر جاهزيته، يسحب نموذج افتراضي اختياري، ثم Streamlit
├── docker/README.md
├── .dockerignore / .env.example
│
├── ui/
│   ├── common.py                # RTL CSS (يشمل إصلاح موضع sidebar عند الطي) + temp_export_dir() + offer_download() + cleanup_stale_temp_dirs()
│   ├── login.py
│   ├── projects.py              # يستخدم temp_export_dir بدل tempfile.mkdtemp المتسرب
│   ├── files.py                 # 🆕 لا تخزين ملفات خام إطلاقاً — قراءة من الذاكرة فقط، والتحديث يطلب إعادة رفع الملف من المستخدم
│   ├── data.py
│   ├── chat.py                  # يدعم النوع الخامس "story"، ويعرض تنبيه auto-fix لأسماء الأعمدة
│   ├── dashboards.py            # 🆕 معرض اللوحات + ٦ قوالب + شريط Slicers قابل للطي + محرر خلايا
│   ├── reports.py               # يستخدم temp_export_dir بدل التسريب
│   └── settings.py              # تبويب "التخزين" أُزيل بالكامل (لم يعد له داعٍ)
│
├── core/
│   ├── auth.py                  # (بدون تعديل جوهري) — sessions table في users.db
│   ├── project_manager.py
│   ├── project_db.py            # 🆕 جداول dashboards/dashboard_cells/dashboard_slicers + إصلاح جوهري لتخزين التاريخ (date columns tracked via settings key، لا تُفقد كـ TEXT بعد SQLite round-trip)
│   ├── file_manager.py          # 🆕 أُعيدت كتابته بالكامل: قراءة bytes من الذاكرة فقط، لا disk I/O إطلاقاً (لا upload/delete/files_dir)
│   ├── data_manager.py          # load_file() يقبل bytes مباشرة، refresh_from_bytes() بدل refresh_from_source، إصلاح change_dtype("date") ليبقي datetime64 حقيقي
│   ├── query_engine.py          # 🆕 تصحيح تلقائي لأسماء الأعمدة الخاطئة (BinderException + Candidate bindings) بفحص تشابه (SequenceMatcher) قبل الثقة، يدعم أعمدة مع/بدون تنصيص
│   ├── dashboard_manager.py     # 🆕 منطق Slicers (جدول→عمود→قيم عبر SQL مباشر) + refresh_dashboard() (الوحيد الذي يُنفّذ أي استعلام فعلي)
│   └── dashboard_templates.py   # 🆕 تعريف القوالب الستة كبيانات (name/description/cell_count/layout_fn)
│
├── ai/
│   ├── base_engine.py / gemini.py / openrouter.py / grok.py / ollama.py  # بدون تعديل جوهري
│   ├── prompt_builder.py        # 🆕 قسم [FILTERS] لحقن قيود Slicers ضمن SQL prompt + build_story() للتحليل النصي
│   └── ai_manager.py            # ask()/tell_story() يمرران filters بالكامل
│
└── exporters/
    ├── chart_renderer.py        # 🆕 رسم PIL بديل لـ matplotlib (bar/line/area/pie + gauge) — يكتشف دعم raqm في Pillow تلقائياً ويتكيف
    ├── pdf_exporter.py          # يستخدم chart_renderer، KPI overlap مُصلَح (leading)، gauge صورة حقيقية بدل نص
    ├── excel_exporter.py        # شيت مستقل لكل رسم (Excel chart حي وقابل للتعديل) + شيت صورة لكل gauge
    ├── report_manager.py / markdown_exporter.py  # بدون تعديل جوهري
    └── (DataLens_User_Guide.docx — دليل مستخدم عربي كامل RTL، ١٣ صفحة، تم تسليمه سابقاً)
```

**جميع هذه الملفات آخر مرة سُلِّمت فيها عبر `present_files` في المحادثة السابقة.**

---

## ⚙️ القرارات التقنية المتفق عليها

1. **لا نص/كود عربي يُعالَج يدوياً (`arabic_reshaper`+`bidi`) عند الرسم عبر Pillow** إذا كانت `raqm` مفعّلة — تُكرر المعالجة وتعكس النص. الكود الحالي يكتشف `PIL.features.check("raqm")` تلقائياً ويتصرف تبعاً لذلك. **لم يُختبر هذا فعلياً على بيئة المستخدم (Windows) بعد.**
2. **لا تخزين لملفات Excel/CSV الخام على السيرفر إطلاقاً.** فقط البيانات النظيفة تُحفظ في `project.db`. "تحديث البيانات" = إعادة رفع الملف من جهاز المستخدم من جديد؛ رسالة خطأ محددة إن لم يُرفَع: `لا يمكن التحديث الآن: الملف غير موجود {file_name}`.
3. **تنظيف تلقائي بالكامل، صفر تدخل يدوي من المستخدم النهائي** (لا تبويب "تخزين"). كل التنظيف (ملفات تصدير مؤقتة، جلسات منتهية) يحدث تلقائياً عند بدء التشغيل.
4. **لوحات المعلومات:**
   - ٦ قوالب فقط، كل قالب = ٤ Gauges أعلى + شكل شبكي مختلف أسفلها (تفاصيل الأشكال في `dashboard_templates.py`)
   - **لا تحديث تلقائي أو فوري لأي شيء** — لا للخلايا ولا للـ Slicers. كل شيء (بيانات + فلترة) يُحدَّث معاً فقط عند ضغط "🔄 تحديث البيانات"
   - الـ Slicers تفلتر على **أي عمود من أي جدول في المشروع** (ليس محصوراً بما يظهر في اللوحة) عبر **حقن الشرط ضمن prompt الذكاء الاصطناعي** لكل خلية (وليس فلترة pandas محلية بعد الجلب) — لأن العمود قد لا يكون أصلاً ضمن نتيجة الخلية
   - شريط Slicers (٤ افتراضياً، قابل للتغيير من `config.py`) في قوائم قابلة للطي، ترتيب الاختيار: **الجدول ← العمود ← القيم**
   - إضافة قالب سابع لاحقاً = إدخال جديد في `DASHBOARD_TEMPLATES` + دالة تخطيط صغيرة في `LAYOUT_REGISTRY` — لا تعديل آخر مطلوب
5. **تصحيح أسماء الأعمدة التلقائي:** فقط عند تشابه حقيقي (`SequenceMatcher` أو احتواء نصي) — رُفض القبول الأعمى لأول اقتراح من DuckDB لتفادي نتائج خاطئة بصمت.
6. **أعمدة التاريخ:** تُسجَّل أسماؤها صراحة عند `save_clean_data` (في جدول `settings`) وتُستعاد كـ `datetime64` فعلي عند `get_clean_data` — لأن SQLite يفقد نوع التاريخ افتراضياً.
7. **Docker:** حاوية واحدة فقط (app + Ollama معاً)، منفذ Ollama (11434) غير مكشوف خارج الحاوية، volume واحد معزول تماماً عن نظام ملفات المضيف.

---

## ▶️ الخطوة التالية فوراً (Next Steps)

**لم يُختبر أي مما يلي فعلياً على بيئة تشغيل حقيقية (لا يوجد Streamlit/Docker/متصفح في بيئة العمل الحالية):**

1. **تشغيل فعلي لصفحة لوحات المعلومات (`ui/dashboards.py`) في متصفح حقيقي** — تحقق خصوصاً من: ترتيب أعمدة RTL البصري (شريط Slicers فعلاً على اليسار كما هو مخطط)، سلوك القوالب الستة بصرياً، وتفاعل التوسيع/الطي.
2. **بناء وتشغيل حاوية Docker فعلياً** (`docker compose up -d --build`) — لم أستطع اختباره (لا يوجد Docker daemon في بيئتي)، تحقق من نجاح البناء وعمل Ollama والتطبيق معاً.
3. **التأكد من سلوك `raqm`/Pillow على السيرفر الفعلي** لرسوم `chart_renderer.py` (النص العربي في الرسوم البيانية والـ Gauges).
4. **مراجعة صفحة الإعدادات** — تحقق أن حذف تبويب "التخزين" لم يكسر أي استيراد متبقٍ.

---

## 💡 اقتراحات لم تُنفَّذ بعد (من نقاش سابق)

### تحسينات تجربة المستخدم (UX)
- سجل أسئلة سابقة + "أسئلة مفضّلة"
- اقتراحات أسئلة تلقائية بناءً على أعمدة الجدول
- Undo/Redo لعمليات تنظيف البيانات
- رفع عدة ملفات دفعة واحدة
- زر "أعد المحاولة بصياغة مختلفة" عند فشل سؤال
- مؤشر تحميل بتقدير وقت (مهم مع Ollama المحلي البطيء)
- وضع ليلي (Dark Mode)
- تلميحات أول استخدام (Onboarding)

### تحسينات التقارير
- قوالب تقارير جاهزة (مبيعات شهري، مخزون...)
- مقارنة تلقائية بين فترتين (KPI فرق ونسبة)
- شعار/هوية الشركة في تصدير PDF
- أرشفة تلقائية للتقارير (نسخة تاريخية عند كل تصدير)
- تعليقات/ملاحظات على بلوكات التقرير
- ترتيب البلوكات بالسحب
- تصدير PowerPoint (PPTX)

### بنية تحتية وأداء
- صلاحيات مستخدمين (مشاهد/محرر)
- سجل تدقيق (Audit Log) — من نفّذ أي استعلام ومتى
- تخزين مؤقت (Caching) لنتائج الاستعلامات المتكررة
- فحص جودة بيانات تلقائي عند الرفع (قيم شاذة/مكررة/فارغة)
- دعم الاتصال المباشر بقاعدة بيانات حية (بدل رفع ملفات فقط)

### لوحات المعلومات — تحسينات لاحقة محتملة (لم تُناقش تفصيلياً بعد)
- "إرسال خلية من لوحة إلى تقرير" (بنفس آلية أزرار المحادثة الحالية)
- تصدير لوحة كاملة (PDF/صورة)
- زر "✍️ إعادة كتابة التحليل" مستقل لكل خلية Story (نوقشت الفكرة ثم أُلغيت لصالح "تحديث شامل فقط" — قد يُعاد النظر فيها لاحقاً لتسريع تجربة التعديل التجريبي)
